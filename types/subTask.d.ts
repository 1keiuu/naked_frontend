declare type SubTask = {
    id: number,
    task_id: number,
    title?: string,
    description?: string,
    status?: string,
    starts_date?: string,
    due_date?: string
    completed_date?: string,
    created_at?: Date,
    updated_at?: Date,
}