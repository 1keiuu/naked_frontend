declare type Epic = {
    id: number,
    title: string,
    description: string,
    starts_date: string,
    due_date: string
    completed_date: string,
    created_at: Date,
    updated_at: Date,
}